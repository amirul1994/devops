name: aws-k3s-ansible
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: A minimal Python Pulumi program
config:
  pulumi:tags:
    value:
      pulumi:template: python
